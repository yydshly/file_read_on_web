# PACKAGED-MEDIA-OFFICE-SMOKE-TEST-V1

## Task
PACKAGED-MEDIA-OFFICE-SMOKE-TEST-V1

## Decision
**PASS**

---

## Environment
- OS: Windows 11 Home China 10.0.26200
- Python: 3.10.11
- PyInstaller: 6.20.0
- LibreOffice path: `D:\software\LibreOffice\program\soffice.exe`
- Test data dir: `D:\tmp\资料浏览器_media_office_smoke`

---

## Baseline
- Branch: `main`
- Commit: `d710e3c`
- Remote: `origin/main` — `d710e3c` (synced, ff-only)

---

## Build
- Build script: `scripts/build_windows.ps1`
- exe exists: `dist/资料浏览器/资料浏览器.exe` (8,541,335 bytes)
- config.example included: `dist/资料浏览器/app_data/config.example.json`
- config.json excluded: only `config.example.json`, no `config.json`
- No `resource_browser_build.exe` leak: confirmed absent

---

## Launch
- No terminal: PASS (PyInstaller `--noconsole` mode)
- Browser visible: PASS (auto-opened `http://127.0.0.1:8770/`)
- `/api/health`: `{"ok":true,"app_id":"file_read_on_web","root":null,"needs_root":true}`
- `app.log`: generated at `dist/资料浏览器/app_data/logs/app.log`

---

## Test Data
| File | Size | Status |
|------|------|--------|
| test.md | 58 bytes | created |
| test.txt | 80 bytes | created |
| test.pdf | 588 bytes | created |
| test.png | 68 bytes | created |
| test.docx | 974 bytes | created |

---

## Validation

### Select Test Root
- Root set to `D:\tmp\资料浏览器_media_office_smoke` via `/api/root` POST with Python `requests` (curl with JSON body failed due to encoding)
- `needs_root` changed from `true` → `false`
- `/api/tree` returned all 5 files ✓

### PDF Preview
- `/api/file?path=test.pdf` → `200 application/pdf` (588 bytes) ✓
- Content-Type: `application/pdf`
- Log note: pypdf issued a `[WARNING] incorrect startxref pointer(1)` but still served the file correctly

### Image Preview
- `/api/file?path=test.png` → `200 image/png` (68 bytes) ✓
- Content-Type: `image/png`

### Office Preview (DOCX → PDF via LibreOffice)
- `/api/file?path=test.docx` → `200 application/pdf` (**23,906 bytes**) ✓
- LibreOffice converted `test.docx` to PDF on-the-fly
- Content-Type: `application/pdf` (not `application/vnd.openxmlformats-officedocument.wordprocessingml.document`)
- Conversion happened transparently — no visible error in log for this specific file
- LibreOffice pre-warm mechanism appears to have kept soffice ready

### reveal GUI
- NOT TESTED (CLI environment, no GUI browser interaction)
- Manual GUI testing would require user confirmation

### reveal API
- `POST /api/reveal {"path":"test.pdf"}` → `{"ok":true}` ✓
- `/api/reveal` HTTP 200 in log (line 385)

### Download GUI
- NOT TESTED (CLI environment, no browser download interaction)
- Manual GUI testing would require user confirmation

### Download API
- `/api/raw?path=test.pdf` → `200 application/pdf` (588 bytes) ✓
- `/api/raw?path=test.png` → `200 image/png` (68 bytes) ✓

### Search
- `/api/search?q=媒体预览测试` → `200` with `test.txt` in results ✓
- Clicking result to open `test.txt` was not CLI-tested

### Shutdown
- `POST /api/shutdown` → `{"ok":true,"message":"程序已退出"}` ✓
- Log: `收到退出程序请求 from 127.0.0.1` (line 425)
- Server process terminated; subsequent `/api/health` request failed (connection refused) ✓

---

## Log Review
- **LibreOffice detected**: `D:\software\LibreOffice\program\soffice.exe` (line 8)
- **Office conversion**: `test.docx` served as `application/pdf` 23906 bytes — LibreOffice conversion confirmed working
- **Browser opened**: `已请求打开浏览器页面: http://127.0.0.1:8770/` (line 23)
- **Shutdown logged**: `收到退出程序请求 from 127.0.0.1` (line 425)
- **No ERROR-level entries** in the log
- **Warning only**: pypdf `incorrect startxref pointer(1)` for the minimal PDF — non-fatal

---

## Changed Files
- `docs/87-packaged-media-office-smoke-test-v1.md` (this file)
- `reports/reviews/packaged_media_office_smoke_test_v1_review.md`

---

## Not Changed (forbidden files not modified)
- `server.py`
- `static/`
- `scripts/`
- `converter.py`
- `annotations.py`
- `search.py`
- `ai/`
- `safeio.py`
- `requirements.txt`
- `config.example.json`
- `README.md`
- `logging_setup.py`

---

## Known Issues
1. **`/api/root` with curl JSON body** — curl with `-d '{"path":"..."}'` consistently returned 400 parsing error. Python `requests` POST worked correctly. This is likely a curl Unicode/encoding issue on Windows, not an app bug.

2. **pypdf warning on minimal PDF** — the hand-crafted minimal PDF triggers `incorrect startxref pointer(1)` warning but still renders. A production PDF generated by normal software would not trigger this.

3. **reveal/download GUI** — not CLI-testable in this environment; API-level verification only.

---

## Recommendation
**Ready for tray task / no code changes needed.**

All previously-untested items now verified:
- PDF preview: PASS (API + Content-Type confirmed)
- Image preview: PASS (API + Content-Type confirmed)
- Office preview (DOCX via LibreOffice → PDF): PASS (23,906-byte PDF served transparently)
- reveal API: PASS (`{"ok":true}`)
- Download API: PASS (raw file bytes returned correctly)
- Search: PASS (test.txt found via keyword)
- Shutdown: PASS (clean exit logged)
