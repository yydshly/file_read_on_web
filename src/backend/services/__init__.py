"""Backend application services.

Available services:
- runtime_state: RuntimeStateStore — state.json persistence + legacy config migration
- tts_cache: TtsCache — TTS audio cache lifecycle
- ai_document: AiDocumentService — AI document eligibility + text extraction
"""
